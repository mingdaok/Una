# UNA 功能清单

> 更新时间：2026-07-31
> 说明：`[x]` 表示代码已实现并通过对应自动化检查；`[ ]` 表示仍需实施或人工验收。

## P0：Live2D 控制链路稳定性

- [x] 统一大模型控制协议，只允许第一行 `EMOTION`、第二行 `ACTION`，提示词不再包含旧 `[动作:...]` 示例。
- [x] 新增流式控制前缀解复用器，支持任意字符分片和连续多个控制帧。
- [x] 混合出现 `[动作:...] + ACTION JSON + 舞台说明` 时完整截获，不进入正文。
- [x] 非法、非对象、重复、残缺 `ACTION` 控制帧安全丢弃，不让 Ticker 或文本流中断。
- [x] 兼容旧版中文动作描述 `EMOTION`，只移除控制头与舞台说明，不丢失同行自然正文。
- [x] 普通聊天和视觉聊天均在字幕、TTS、数据库、流结束消息和记忆服务前增加最终文本清洗边界。
- [x] 后端停止发送用于聊天动作的预设 `motion_file`。
- [x] 前端删除按情绪调用 `model.motion()` / `model.expression()` 的预设动作路径。
- [x] 模型加载时调用 `expressionManager.resetExpression()` 正式复位，不再使用无参数 `model.expression()` 随机表情。
- [x] 模型加载时通过 Cubism4 正式的 `setParameterValueByIndex()`，把包括 `Param5`、`Param6` 在内的全部 CoreModel 参数恢复到各自默认值。
- [x] 新增历史控制文本迁移工具；扫描、计算和更新均受同一写锁保护，避免覆盖并发消息；无法可靠分离正文的记录保持原样，并通过 `skipped` 单独报告给人工处理。
- [x] 真实数据库 3 条泄漏记录已完成清理；旧格式误清洗记录已从备份完整恢复并用修正版重新处理，最终复查 `matched=0`、`skipped=0`。
- [x] 保留迁移前备份 `una_memory.before-control-clean-20260726-144001-167030.db` 和修正版迁移备份 `una_memory.before-control-clean-20260726-150656-360962.db`。
- [x] 后端完整回归通过：49 项。
- [x] 前端完整回归通过：12 项。
- [x] 完成前端生产构建并同步到 `backend/static/mobile`，入口引用 `index-BBNF1xQ3.js`，前后端产物 SHA-256 一致。
- [x] P0 提交 `1418a61` 已快进合并到本地 `main`，并在合并后的主目录重新通过后端 49 项、前端 12 项测试。
- [ ] 在真实浏览器中连续发送至少 10 条消息，确认字幕不出现控制文本、模型不再停留于 `poke_face` 等预设姿势。
- [ ] 在 HBuilderX Android APK 中完成同样的真机回归。

## P1：AI 自由驱动 Live2D

- [x] 前端 v3 动作协议校验与关键帧编译器：拒绝过期/非有限/嘴部通道输入，逐轨隔离异常并按目标关键帧 easing 安全采样。
- [x] 已确认 P1 双阶段混合驱动设计：用户明确动作即时执行，AI 回复使用归一化连续通道和稀疏关键帧。
- [x] 已明确 P1 隔离规则：设计和代码只进入 `codex/p1-live2d-motion-v3` 分支，未经确认不合并 `main`。
- [x] 中文设计文档已写入并完成占位符、矛盾、范围和歧义自检；提交后等待用户书面审阅。
- [x] 中文 TDD 实施计划已写入，覆盖后端协议、即时指令、关键帧、能力映射、状态混合、兼容、构建和验收。
- [x] 后端生成结构化 `live2d_action_v2` 动作事件。
- [x] 后端 v3 协议校验与导演。
- [x] WebSocket 将动作事件与文字回复分离传输。
- [x] v2 兼容动作编译模块已完成：将 8 种旧意图、强度和随机种子转换为 v3 归一化语义轨道，保留 `compileAction` 别名，且不再输出嘴部或模型物理参数；已由 Task 9 统一控制器接入并消费。
- [x] 用户发送入口已本地优先：明确中文动作指令生成 `user_command` v3 轨道，否定句和普通文本生成 `local_micro_reaction` v3 倾听反应，均在 WebSocket 文本发送前写入状态。
- [x] 前端 WebSocket 已接收、归一化并按 `motion_id` 去重 v3 动作；过期或畸形事件丢弃，去重缓存按有效期清理且容量有界，并隔离过期连接回调。
- [x] 应用层已将 `motionEvent` 传递给 Live2DViewer；Task 9 统一控制器已在 Live2D 原生 `internalModel.update()` 完成后、当前绘制前的 post-update 后处理阶段消费真实 v3 动作，并继续兼容 v2 与旧 `chat_action`。
- [x] 动作参数不接管嘴部参数，语音口型保持最高优先级。
- [x] 新旧动作执行路径分离，自由动作不再读取旧版 `params`。
- [x] 修复自由动作触发后 `ao.params.direction` 导致 Live2D Ticker 停止的问题。
- [x] 单个动作采样器异常时自动丢弃当前动作，呼吸、眨眼和口型 Ticker 继续运行。
- [x] 动作时长缺失或非法时使用 800 毫秒安全默认值。
- [x] 增加 Live2D 控制器回归测试，覆盖无 `params`、采样异常和时长缺失。
- [x] 完成前端生产构建并同步到 `backend/static/mobile`。
- [x] AI 直接输出连续动作通道或关键帧，而不是从 8 种固定意图配方中选择。
- [x] 即时中文动作指令可安全解析为串行/并行动作组，轻重幅度修饰相对各动作基础振幅生效；同通道并行冲突或无法在 12 帧内完整回中的组合安全拒绝。
- [x] 运行时模型能力映射已接入控制器：兼容 Cubism4 的私有 ID 列表形态，逐参数隔离范围读取异常，语义通道、口型和呼吸分别安全投影；`Live2DViewer` 只在真实新模型加载完成后发送实例/版本令牌，慢加载不会超时失效，也绝不以新模型名绑定旧 CoreModel。
- [x] 单一 `Live2DStateMixer` 已接入 Live2D 原生 `internalModel.update()` 后处理阶段：v3、v2、本地微反应和旧 `chat_action` 兼容适配均先编译为语义轨道；每帧由 UNA 在原生参数更新完成后投影覆写，动作结束自动回归基础层；同来源同通道的新 override 在默认 140 毫秒交叉淡化后永久截断旧通道，非重叠通道继续运行。
- [x] 已确认并修复渲染顺序根因：旧 `-25` 优先级 Ticker 位于 PIXI 绘制之后，UNA 动作写入会被下一帧 Live2D 原生更新覆盖；控制器现已停止通过该 Ticker 写入参数。
- [x] 真实渲染顺序回归测试已锁定“原生重置 → UNA 覆写 → 绘制”，并覆盖模型切换、重复 `ready`、卸载和单帧异常后的恢复。
- [x] 口型和呼吸为保留层：任何动作轨道均不能写入嘴部参数；TTS 仍在每帧末尾安全投影 `ParamMouthOpenY` 与 `ParamMouthForm`，所有写入均为有限值且受模型实际范围限制。
- [x] Task 10 自动化回归完成：后端 58 项、前端 109 项通过；生产构建成功并已发布到 `backend/static/mobile`，入口引用存在的 `assets/index-Cb5YTatm.js`。
- [x] 2026-07-29 渲染顺序修复全量验证完成：后端 58 项、前端 120 项通过；生产构建和 PowerShell 发布成功。当前入口引用 `assets/index-CaN8btbP.js` 与 `assets/index-DY8emamO.css`，前后端 SHA-256 分别一致为 `65346A0CDB91429C0910FFA02BA0AE1A5C1BDAC0FBD4096BD0F44579B2D634AD` 和 `43F47B70339A913F280D4AD0FB246E3EEF80145E72C375291D0CDF95CBE13AC1`。
- [x] WebSocket 发布边界回归覆盖：去重缓存满 100 条时淘汰最早 ID、旧连接代次事件不改写新连接状态、畸形 v3 事件不改写当前可用动作。
- [x] 静态安全检查完成：前端运行时代码不存在 `getParameterId()` 调用；后端 v3 白名单和即时动作生成器不存在嘴部控制通道。
- [x] 2026-07-29 最终整分支审查已从 base 精确恢复 `frontend_react/dist/assets/index-BBNF1xQ3.js` 与 `frontend_react/dist/assets/index-vhYxHOKu.css` 两个旧哈希资产，并统一清理独立 Ticker 的过时描述；当前入口与业务逻辑未改动。
- [x] 已检查两个现有模型的真实手臂与手部能力，并确认模型专属动作设计：Hiyori 使用标准左右臂和手部参数；panda_cake 使用熊猫、捧脸开关及对应物理参数；两个模型不做语义错误的动作替换。
- [x] 已确认模型 Action Profile 与本地随机动作调度设计：动作逻辑只使用语义通道，按模型能力、冷却时间、最近动作防重复和会话随机种子调度；尚未实施。
- [ ] Hiyori 举手/挥手与 panda_cake 抱熊猫/捧脸尚未实施；需完成前后端模型能力过滤、即时口令、AI 自动动作、参数复位、自动化回归及桌面人工验收。
- [ ] ~~已在真实桌面浏览器完成 10 轮核心对话验收，且动作均可见。~~ 此旧人工验收结论已被 2026-07-29 用户复测推翻，不能作为渲染顺序修复已通过的证据；须以下方本轮九项桌面验收重新确认。
- [ ] 在 HBuilderX 打包后的 Android APK 中完成同样的真机回归测试。

### P1 Task 10 历史人工验收记录（2026-07-29，结论已被复测推翻）

> 以下记录仅保留历史过程；其中“桌面动作可见”的判断已被 2026-07-29 用户复测推翻，不代表当前验收状态。

- [x] 桌面浏览器后端：使用 `D:\ai\python 3.11\python.exe backend\main_server.py` 成功启动。ASR、记忆、调度器和 WebSocket 正常，监听端口 8000。GPT-SoVITS 的 9880 服务未启动时，系统自动降级到 Edge TTS；Rhubarb 唇形切片和音频获取正常。
- [x] 临时测试账号：仅在隔离工作树测试数据库注册 `codex_p1_test_20260729`，未改动主数据。`mingdaok` 登录失败是隔离数据库没有主目录的迁移数据，不记为产品缺陷。
- [x] 10 轮核心对话：已实测“左右摇头三次”“轻轻点头两次”“不要点头/摇头”、普通陪伴对话、“明显向左看”“快速眨眼三次”“先低头再抬头”、上下文回忆、“点头五次”和结束对话。消息、流式文字和语音均正常，模型无卡死；否定句未触发被否定的显式动作，上下文能准确回忆动作。
- [x] 即时动作：panda_cake 的摇头、点头、看向和眨眼均有可见参数动作；AI 回复期间模型持续动作，文字和音频正常。
- [x] 模型切换：设置中已切换至 Hiyori，随后“点头两次”有可见动作；旧动作未异常重播。
- [x] WebSocket 重连与后台恢复：页面重新加载后日志显示旧连接断开、新 ticket/refresh 与 WebSocket 重新 accepted，模型和历史继续可用；在新空白页停留约 2.5 秒后返回，模型仍持续呼吸、眨眼且未冻结。
- [x] 计划 1134 并行动作：在 Hiyori 发送“请慢慢摇头两次并眨眼”，约 1 秒连续采样 4 帧，头部以慢速左右交替并完成两次节奏，眼睑同时闭合/张开；两个通道并行且无卡死。
- [x] 计划 1137 旧 `motion_id` 不重播：该动作发出后立即 reload，日志仅见旧 WS 关闭、`GET /history`、新 ws-ticket/refresh 与新 WS accepted，未见同一用户消息或旧动作事件；新 Hiyori ready 后约 1.2 秒连续采样 4 帧，仅有基础呼吸、自然轻微姿态与 AI 语音口型，未重播旧“两次慢摇头+眨眼”序列，console error/warn 仍为 0。
- [ ] HBuilderX Android APK：必须由实际设备确认；桌面结果不能替代真机验收。

### P1 渲染顺序修复桌面验收（2026-07-29）

> 本轮在真实桌面浏览器访问 `http://127.0.0.1:8000/`，入口加载 `index-CaN8btbP.js` 且 WebSocket 连接成功；连续截图帧和带时间戳控制台记录保存在本次浏览器工具输出中。

- [x] “请轻轻点头两次”：正式发送后约 0.10、0.35、0.70、1.20 秒连续截图显示 Hiyori 头部和上身下压、回中、再次变化，两次点头在当前绘制中可见。
- [x] “请慢慢左右摇头两次并眨眼”：约每 240 毫秒采样 8 帧，头部和身体左右往返；首帧半闭眼，第 7 帧再次闭眼，组合动作可见。
- [x] “请不要点头，也不要摇头”：隔离等待 3.5 秒清空前一动作后复测，约每 260 毫秒采样 6 帧均保持居中，仅有自然眨眼，无点头或摇头轨迹。
- [x] 普通陪伴对话与自然微反应：在 Hiyori 上发送不含显式动作词的“今天有点累，想安静地和你待一会儿”，约每 280 毫秒连续采样 9 帧；头部和上身先向画面右侧轻微倾斜再缓慢回摆，眼睑下垂、自然闭眼后重新睁开，并伴随细微头部和身体漂移，证明 TTS 口型以外的自然微反应持续工作。
- [x] 语音播放：控制台记录音频预加载和播放成功；回复期间截图中 Hiyori 嘴部明显张开，TTS 口型持续工作。
- [x] 模型切换：先从 Hiyori 切换至粉色熊猫并等待就绪，再由粉色熊猫切换回 Hiyori 并等待约 2.2 秒；在新 Hiyori 实例发送“请轻轻点头一次”，首次约每 240 毫秒采样 6 帧、隔离复测约每 180 毫秒采样 7 帧，均显示头部和上身由向右下倾斜逐步回中，期间闭眼或半闭眼，随后稳定回到中位，证明新模型继续执行动作；最终保持 Hiyori。
- [x] 刷新页面：动作结束后重新加载，约每 260 毫秒采样 4 帧模型稳定，无旧摇头动作重播；历史文本和仍在到达的 AI 回复恢复显示，但动作事件未重播。
- [x] 后台停留 5 秒返回：打开空白标签使应用后台停留 5.2 秒，返回后发送“请轻轻点头一次”，约 220 毫秒和 600 毫秒帧显示模型再次发生可见位移，更新链路已恢复。
- [x] 浏览器控制台：筛选 `Post-update` 结果为空，无持续 post-update 异常；首次加载存在一条既有“获取历史失败：无法获取聊天记录”错误，与本次动作渲染顺序无关。
- [ ] HBuilderX Android APK：必须由实际设备确认；桌面结果不能替代真机验收。

## P2：模型专属动作与调度

- [x] Task 1 已实现并通过定向自动化测试：前端建立 Hiyori 与 panda_cake 的模型专属动作档案、通用语义通道、按模型过滤和按通道数值范围校验；panda 姿态通道统一使用可实际投影的 `0..1` 激活度，旧版未提供模型名的动作协议调用仍兼容校验全部已知通道。
- [x] Task 2 已实现并通过定向自动化测试：后端按精确模型名校验 AI v3 动作通道和值域，panda 负激活值会在解析和二次过滤阶段明确拒绝，提示词同步声明 `0..1`；未知或缺失模型仅保留通用通道，ControlPrefixDemux 在解析后执行独立的第二道模型过滤，控制块不进入可见正文。
- [x] Task 3 已实现并通过定向自动化测试：WebSocket 文本消息规范化并传递当前模型名；AI 提示词、流式控制块拆解和 v3 动作投递均按模型只允许对应通道，未知或缺失模型回退通用通道，跨模型 ACTION 不会投递或泄漏到正文。
- [x] Task 4 已实现并通过定向自动化测试：前端通过共享模型选择 API 同步当前模型；每条 WebSocket 文本均携带 `live2d_model`；即时口令只为选中模型生成对应的 v3 语义通道，跨模型或否定口令静默无动作，普通文本仍触发 listening 微反应，且不生成 mouth 通道。
- [x] Task 5 已实现并通过定向自动化测试：专属语义通道只在原生 `update()` 后、当前绘制前安全投影为真实 Cubism 参数；Hiyori 仅在 `PartArmA` 可见时相对抬臂并门控挥手，panda_cake 的抱熊猫/捧脸姿态互斥、按语义包络恢复默认值，缺失参数或可选物理细节均安全跳过；TTS 口型仍为本帧最后写入层，模型、ready、卸载和 WebSocket generation 重置均会清除运行中的专属轨道。
- [x] Task 6 已实现并通过定向自动化测试：单一 post-update 入口以会话种子和单调时钟调度低优先级 `local_random` v3 语义事件；每次 reset 后普通候选先等待 3–6 秒、专属候选先等待 15–25 秒，首帧不会触发；匹配情绪的专属候选到期后还需通过固定 20% 的种子化概率门，未命中也推进下一截止点，同一会话种子可复现。最近三个动作家族不重复；用户命令或 AI 回复活跃、未知模型、跨模型动作及 mouth 轨道均不会产生本地动作。
- [ ] 后续仍待桌面浏览器与 HBuilderX Android APK 人工确认 Hiyori 举手/挥手及 panda_cake 抱熊猫/捧脸动作实际可见、结束后恢复默认值；APK 尚未验证。
- [x] P2 自动修复版已完成前端生产构建并通过 `scripts/publish_frontend.ps1` 同步到 `backend/static/mobile`；两个入口均引用 `index-DWDv-qYU.js` / `index-CrppLWLv.css`，对应文件 SHA-256 一致。

### P2 运行时修复（2026-07-29）

- [x] Hiyori 抬臂可见性兼容：Live2D 运行时优先按部件 ID 读取 `PartArmA` 透明度；若运行时仅提供 Cubism 标准的“部件 ID → 索引 → 透明度”接口，也能识别可见手臂并写入 `ParamArmLA/ParamArmRA`。已新增回归测试，防止“命令已识别但手臂被错误判定不可见”的静默失效。
- [x] panda_cake 姿态停留：`抱熊猫`/`熊猫手` 与 `捧脸`/`戳脸` 不再复用瞬时手势曲线；改为 180ms 进入、3 秒保持、250ms 自然恢复，共 3430ms，并保持两种姿态互斥。

## P0：克隆语音连续播放与精确口型同步

- [x] 已完成现有 UNA → GPT-SoVITS → Rhubarb → WebSocket → 前端播放链路分析，确认主要停顿来自重复完整请求、GPT-SoVITS 二次切分与默认约 0.4 秒片段静音、后续音频未及时准备造成的队列断粮。
- [x] 已确认采用“短首段 + 后续文字合并 + 单会话有序预生成 + 完整音频 Rhubarb 精确口型”方案，不以音量驱动粗略口型替代现有精确同步。
- [x] 中文设计文档已写入并通过用户书面审阅，覆盖请求参数、文字聚合、调度队列、精确口型、结束语义、失败降级、阶段耗时日志和验收标准。
- [x] 中文 TDD 实施计划已写入，拆分为 GPT-SoVITS 参数与计时、文字聚合、有序语音会话、后端投递、前端预加载队列、同步口型播放器和全量验收七项任务；等待用户选择实施方式。
- [x] 实施前测试审查已完成：任务 4 已按用户确认由源码/AST 变化检测改为真实异步行为测试，直接验证结束等待、中断取消、旧回复隔离和 vision 复用。
- [x] GPT-SoVITS 短语音请求改为 `cut0`，`fragment_interval` 降至约 0.05 秒，并为超长文本提供统一回退策略。（代码已实现、定向测试通过）
- [x] 已实现可独立测试的纯语音文字聚合器：首段非空立即封口，后续按情绪隔离并聚合为约 40～60 字窗口，支持 80 字硬上限、200ms 截止排空及会话关闭尾段排空。
- [ ] 实现单会话有序语音调度器。
- [ ] 调整 `audio_stream_end` 语义，使其等待全部语音作业进入终态。
- [ ] 前端实现后续音频预加载、严格有序连续播放、断粮统计和异常安全跳过。
- [ ] 保留完整音频 + Rhubarb 音素时间轴，确保音频与 Live2D 嘴型使用同一 `AudioContext` 播放起点。
- [x] GPT 请求、文件写入、Rhubarb 和转码阶段耗时日志已实现，并避免记录令牌或完整媒体票据。（代码已实现、定向测试通过）
- [ ] 增加聚合、队列、投递和前端播放阶段耗时日志，并避免记录令牌或完整媒体票据。
- [ ] 完成后端与前端自动化回归、桌面浏览器 10 轮人工验收；HBuilderX Android APK 真机验收单独记录。

## P0 人工验收标准

1. 回复字幕、语音和历史记录中不出现 `ACTION:`、`EMOTION:` 或 `[动作:...]`。
2. AI 开始回复前可以看到轻微倾听反应。
3. AI 回复过程中动作、文字和语音能够同时工作。
4. 回复结束后模型继续呼吸和自动眨眼，不停留在 `poke_face`、爱心眼等预设姿势。
5. 连续发送 10 条消息后，Live2D Ticker 不停止。
