# 安全说明

## 本地私有文件

以下内容只能保存在本地，不能提交到 Git：

- `config.yaml` 与各类 `.env` 本地覆盖文件；
- SQLite 数据库与 Chroma 长期记忆库；
- 用户语音、朋友圈图片、日记图片和生成图表；
- 日志、缓存、临时音频与依赖目录。

仓库已经通过 `.gitignore` 排除这些路径。首次运行前，请复制
`config.example.yaml` 为 `config.yaml`，并只在本地填写密钥。

## 凭据泄露处理

如果密钥曾经进入 Git 提交，即使随后删除文件，也应立即在对应服务商
控制台撤销并重新生成凭据。不要继续使用已经提交过的旧密钥。

## 报告安全问题

请不要在公开 Issue 中粘贴密钥、访问令牌、数据库内容、用户对话或媒体文件。
